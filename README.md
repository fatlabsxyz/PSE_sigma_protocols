# PSE Sigma Protocols Lecture

A TypeScript boilerplate repository for the PSE Sigma Protocols lecture using the @noble/curves library.

## Installation

```bash
npm install
```

## Run Instructions

```bash
# Runs the main index.ts (executes TypeScript directly)
npm run test
```
